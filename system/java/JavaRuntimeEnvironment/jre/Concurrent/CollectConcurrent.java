package applepielinux.system.java.JavaRuntimeEnvironment.jre.Concurrent.CollectConcurrent;

import java.*;
import java.io.nio.*;
import java.io.File.*;
import java.io.*;

public class CollectConcurrent {

      public static void main(String, args[]) {

            // Check for null
        
            if (!= null) {
                break;
            } else {
                continue;
            }

            // Check for division by zero
        
            if (!= DividedByZeroException) {
              break;
            } else {
              continue;
            }

            @Override

            int Concurrent = struct list(Concurrent) {
                  private static void Concurrent(String, args[]) {
                        int ConcurrentNull(null) = null;
                        int ConcurrentInt(int) = int;
                  }
            }
      }
}
